# AI Agent System — Web UI

React + Vite SPA for graphically customizing agents and workflows.

```bash
npm install
npm run dev     # http://localhost:5173, proxies /api to the backend on :8000
npm run build   # production build -> dist/
```

See the repository root `README.md` ("Web UI: Graphical Agent Customization") for details.
